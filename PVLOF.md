# PVLOF V1.6 技术交接说明

> 用途：向新的开发人员或 AI 工具完整说明本项目当前 PVLOF V1.6 的数据假设、标定方法、数学定义、决策流程、输出语义和已知盲区。本文描述的是当前已经实现的算法，不把尚未实现的 V1.7 设想写成既有功能。

## 1. 问题定义

项目目标是在没有可靠人工故障标签的前提下，检测光伏逆变器内部的**非零低电流组串**。

输入数据按逆变器、时间点存储，主要字段为：

- `plant_id`：电站编号；
- `device_no`：逆变器序列号；
- `event_time`：采样时间；
- `string_current_01 ... string_current_30`：各组串电流；
- `string_status_01 ... string_status_30`：各组串原始状态；
- `main_string_count`：数据源声明的组串数量；
- `forecast_ghi`：天气预报水平总辐照度，粒度通常为 15 分钟。

设备电流数据通常为 5 分钟粒度。

本算法刻意把以下问题分开：

1. **零电流**：不由 PVLOF 判断，使用显式规则处理，例如 `current <= 0`；
2. **非零低电流**：由 PVLOF V1.6 判断；
3. **整台逆变器整体欠流**：当前版本没有完整的独立设备级告警分支；
4. **过高电流**：当前版本不是主要检测目标。

因此，下文中的“欠流”默认指电流大于 0、但相对于合理参考明显偏低的组串。

---

## 2. V1.6 的总体结构

V1.6 不是一个单独的 LOF 公式，而是一条组合流水线：

```text
历史电流与状态
  ↓
推断每台逆变器真实配置的组串通道
  ↓
计算同站、留一逆变器的虚拟辐照度
  ↓
使用 forecast_ghi 对虚拟辐照度进行有限度条件化
  ↓
标定每条组串的电流响应系数
  ↓
计算期望电流和归一化残差
  ↓
├─ 孤立欠流分支：LOF + 方向 + V1.6 物理门控
└─ 群体欠流分支：排序残差 + 最大断层 + 群体连续性
  ↓
设备/电站/组串数分层 LOF 阈值
  ↓
3 个连续 5 分钟点确认
  ↓
确认后记忆；连续 3 个正常点清除
  ↓
点级结果与事件级结果
```

算法的核心思想是：

> 先用同站其他逆变器估计当前光照背景，将每条组串电流转换为相对于自身历史响应的无量纲残差，再同时检测孤立异常和少数群体异常。

---

## 3. 通道清单：不直接相信 `main_string_count`

历史数据中曾出现：

- `main_string_count=19`；
- 但 20、21 号组串长期存在有效电流和状态记录。

如果直接假设有效组串为 `1 ... main_string_count`，会漏掉真实存在的非连续或超出计数字段的组串。

当前实现按电站和逆变器建立固定通道清单：

\[
\mathcal C_d = \{i:\text{channel }i\text{ has sufficient historical evidence}\}
\]

证据优先级为：

1. 如果该通道有状态字段，则历史上出现非零状态视为物理通道存在的权威证据；
2. 如果状态字段不可用或全空，才使用历史正电流作为回退证据；
3. 证据需要满足最小样本数和最小日期数；
4. 清单允许非连续，例如 `[1,2,3,5,8]`。

之后所有中位数、LOF、群体检测和期望电流计算只使用 `configured_strings` 中的组串。

对应实现：`src/pv_anomaly/pvlof_channels.py`。

---

## 4. 同站虚拟辐照度

### 4.1 为什么不直接使用原始电流

组串电流随太阳辐照度快速变化。直接对原始电流做横向或时间比较，会把云层、早晚光照和天气变化误判为故障。

当前算法先把同站其他逆变器作为“软件辐照度计”，构造无量纲虚拟辐照度。

### 4.2 逆变器代表电流

对逆变器 \(d\) 在时间 \(t\) 的所有已配置、正电流组串取中位数：

\[
M_{d,t}=\operatorname{median}_{i\in\mathcal C_d,\ I_{d,i,t}>0} I_{d,i,t}
\]

使用中位数而不是平均值，是为了降低少量异常组串的影响。

### 4.3 设备尺度

不同逆变器容量、组串数量和电流量级不同。每台逆变器使用历史代表电流的 95% 分位数作为尺度：

\[
S_d=Q_{0.95}\left(M_{d,t}\right)
\]

归一化后的设备输出为：

\[
U_{d,t}=\frac{M_{d,t}}{S_d}
\]

### 4.4 留一逆变器虚拟辐照度

检测目标逆变器 \(d\) 时，不能用它自己构造环境参考，否则整机异常会污染参考。算法使用同站其他逆变器的中位数：

\[
G^{peer}_{d,t}
=
\operatorname{median}_{j\ne d} U_{j,t}
\]

这是 leave-one-inverter-out 设计。

只有同站有效参考逆变器数量达到 `minimum_peer_devices` 时，该参考才可用。当前典型最小值为 5。

---

## 5. `forecast_ghi` 条件化

### 5.1 作用边界

`forecast_ghi` 是天气预报的水平总辐照度，不是现场气象仪实测值。当前算法不让它直接替换同站设备参考，而是作为：

- 同站参考不足时的回退；
- 同站参考存在时的有限修正；
- 大范围同步天气变化的辅助背景。

同站逆变器仍然是主要信号。

### 5.2 时间语义选择

每个电站分别尝试候选偏移，例如：

- `source_offset_minutes = 0`：把 GHI 解释为当前时刻；
- `source_offset_minutes = 15`：把 GHI 解释为未来 15 分钟。

选择与历史 `G_peer` Spearman 相关性更高、且样本数满足要求的时间语义。

### 5.3 GHI 到虚拟辐照度的单调映射

由于 GHI 的单位与虚拟辐照度不同，不能直接相加。按电站建立经验映射：

\[
G^{forecast}_{p,t}=f_p(\operatorname{forecast\_ghi}_{p,t})
\]

其中 \(f_p\) 由分位数区间的中位数锚点构成，并强制为单调非下降映射。

### 5.4 有界融合

当同站参考和天气预报同时存在时，先限制天气值相对于同站参考的修正幅度。当前典型上限为 ±20%：

\[
\widetilde G^{forecast}_{d,t}
=
\operatorname{clip}
\left(
G^{forecast}_{p,t},
0.8G^{peer}_{d,t},
1.2G^{peer}_{d,t}
\right)
\]

然后按有效同站逆变器数量确定权重：

\[
G^*_{d,t}
=w_{d,t}G^{peer}_{d,t}
+(1-w_{d,t})\widetilde G^{forecast}_{d,t}
\]

同站参考越充足，\(w\) 越大。当前典型范围约为 0.65 至 0.85。

回退关系为：

- 只有同站参考：使用 `G_peer`；
- 只有可用天气映射：使用 `G_forecast`；
- 两者都有：有界加权融合；
- 两者都没有：该时刻不可评估。

对应实现：`src/pv_anomaly/pvlof_v12.py`。

---

## 6. 组串响应、期望电流和残差

### 6.1 组串响应系数

对每个电站、逆变器和组串，在历史标定数据中计算：

\[
a_{d,i}
=
\operatorname{median}_t
\left(
\frac{I_{d,i,t}}{G^*_{d,t}}
\right)
\]

这里 \(a_{d,i}\) 表示该组串相对于虚拟辐照度的典型电流响应。

### 6.2 期望电流

检测时刻的期望电流为：

\[
\widehat I_{d,i,t}=a_{d,i}G^*_{d,t}
\]

### 6.3 无量纲残差

定义：

\[
r_{d,i,t}
=
\frac{I_{d,i,t}}{\widehat I_{d,i,t}}
\]

解释：

- \(r\approx1\)：符合历史响应；
- \(r<1\)：低于期望；
- \(r>1\)：高于期望。

同一逆变器、同一时刻的残差中位数为：

\[
\widetilde r_{d,t}
=
\operatorname{median}_{i}r_{d,i,t}
\]

`residual_median` 用于判断异常方向，避免把高电流密度异常误报成低电流。

---

## 7. 一维 LOF

PVLOF 在同一逆变器、同一时刻的组串残差集合上计算一维 LOF。

设目标组串残差为 \(r_i\)，距离为：

\[
d(i,j)=|r_i-r_j|
\]

当前典型邻居数：

\[
k=5
\]

### 7.1 可达距离

\[
\operatorname{reach\_dist}_k(i,j)
=
\max\left(
d(i,j),
k\text{-distance}(j),
\epsilon
\right)
\]

其中 \(\epsilon\) 是距离下限，用于防止大量相等电流导致密度无穷大。

### 7.2 局部可达密度

\[
\operatorname{LRD}_k(i)
=
\left[
\frac{1}{|N_k(i)|}
\sum_{j\in N_k(i)}
\operatorname{reach\_dist}_k(i,j)
\right]^{-1}
\]

### 7.3 LOF 分数

\[
LOF_k(i)
=
\frac{1}{|N_k(i)|}
\sum_{j\in N_k(i)}
\frac{\operatorname{LRD}_k(j)}
{\operatorname{LRD}_k(i)}
\]

通常：

- \(LOF\approx1\)：局部密度与邻居相近；
- \(LOF>1\)：比邻居更稀疏；
- 分数越高，密度异常证据越强。

重要限制：LOF 衡量的是密度异常，不等价于电流损失严重程度。多个低电流组串聚集在一起时，它们会互相成为邻居，LOF 可能仍接近 1。

对应实现：`src/pv_anomaly/pvlof.py::_lof_1d`。

---

## 8. 分层 LOF 阈值

### 8.1 标定样本

标定时只保留满足以下条件的样本：

- PVLOF 可评估；
- 组串响应已知；
- 电流大于 0；
- LOF 和残差有限；
- 残差低于同期残差中位数，即方向为低；
- 满足当前启用的物理效果条件；
- 已知告警区间及前后缓冲区从标定数据中排除。

### 8.2 99.5% 上尾分位数

全局阈值为正常方向性样本 LOF 的 99.5% 分位数：

\[
\tau_{global}=Q_{0.995}(LOF)
\]

### 8.3 分层回退

阈值按以下层级选择：

```text
具体设备
  → 电站 + 可用组串数
    → 可用组串数
      → 全局
```

设备级阈值通常要求至少 10000 个样本和 20 个日期，否则回退到上一级。

### 8.4 收缩估计

局部样本阈值不会直接采用，而是向父级阈值收缩：

\[
w=\frac{N}{N+K},\qquad K=5000
\]

\[
\tau_{local}
=wQ_{0.995}(LOF_{local})
+(1-w)\tau_{parent}
\]

这样可以减轻小样本设备阈值剧烈波动。

例如当前某台设备可能得到 `2.905368`，这是设备历史分位数与父级阈值的加权结果，不是人工直接指定。

对应实现：`src/pv_anomaly/pvlof_v2_hier.py`。

---

## 9. V1.6 孤立欠流分支

### 9.1 欠流方向

定义相对同期中位数的下跌比例：

\[
D^{rel}_{d,i,t}
=
1-\frac{r_{d,i,t}}{\widetilde r_{d,t}}
\]

只有：

\[
r_{d,i,t}<\widetilde r_{d,t}
\]

才被认为方向为低。

### 9.2 绝对电流缺口

\[
D^{abs}_{d,i,t}
=
\widehat I_{d,i,t}-I_{d,i,t}
\]

### 9.3 V1.6 混合物理门控

当前 V1.6 使用 OR 规则：

\[
E_{d,i,t}
=
\left(D^{rel}_{d,i,t}\ge 20\%\right)
\lor
\left(D^{abs}_{d,i,t}\ge0.5A\right)
\]

该规则用于兼顾：

- 高电流阶段：0.5 A 可能很小，依靠相对比例；
- 低电流阶段：相对比例容易被放大，依靠绝对差值；
- 中间阶段：任一物理差异足够明显即可保留候选。

### 9.4 孤立原始候选

孤立候选可以概括为：

\[
C^{iso}_{d,i,t}
=Eligible
\land I_{d,i,t}>0
\land ResponseKnown
\land LOF_{d,i,t}\ge\tau_{context}
\land r_{d,i,t}<\widetilde r_{d,t}
\land E_{d,i,t}
\]

注意：V1.6 中 LOF 仍然是必须通过的前置条件。物理门控只能过滤 LOF 候选，不能救回 LOF 未达阈值但物理缺口巨大的组串。

---

## 10. 群体欠流分支

孤立 LOF 无法可靠检测多个组串一起下降的情况，因此 V2 之后增加了独立群体分支。

### 10.1 排序残差

对同一逆变器同一时刻的有效残差排序：

\[
r_{(1)}\le r_{(2)}\le\cdots\le r_{(n)}
\]

相邻断层为：

\[
\Delta_k=r_{(k+1)}-r_{(k)}
\]

### 10.2 当前切分规则

从满足最小低侧组串数的所有切分中，先寻找全局最大断层：

\[
k^*=\arg\max_k\Delta_k
\]

低侧候选群体为：

\[
\mathcal G_{low}=\{(1),\ldots,(k^*)\}
\]

当前典型约束：

- 最少低流组串数：2；
- 最大低侧比例：60%；
- 最大断层需要超过标定阈值；
- 低侧残差中位数需要低于残差严重度阈值。

群体原始候选条件可写为：

\[
C^{group}_{d,i,t}
=Eligible
\land i\in\mathcal G_{low}
\land \Delta_{k^*}\ge\tau_{gap}
\land \operatorname{median}(r_i:i\in\mathcal G_{low})\le\tau_{residual}
\land I_{d,i,t}>0
\]

其中：

- \(\tau_{gap}\) 通常由历史最大断层的 99.5% 分位数标定，并设最小下限；
- \(\tau_{residual}\) 通常由历史残差的低分位数标定。

### 10.3 群体时间连续性

群体成员在相邻时刻可能略有变化，因此不要求成员集合完全相同。使用 Jaccard 相似度：

\[
J(A_t,A_{t-1})
=
\frac{|A_t\cap A_{t-1}|}{|A_t\cup A_{t-1}|}
\]

当前典型连续条件：

\[
J\ge0.5
\]

并要求达到规定的连续时间点数。确认后，只对当前仍属于群体的组串输出 `collective_member_alert`。

### 10.4 当前群体分支的重要限制

当前实现只选择一个全局最大断层。如果最大断层的低侧超过 60%，算法直接拒绝本次群体候选，不会继续搜索低侧内部的第二级断层。

因此，对如下多层分布可能漏检：

```text
严重低流组 | 中度低流组 | 正常/高电流组
```

对应实现：`src/pv_anomaly/pvlof_v2.py`。

---

## 11. 严格连续确认和 V1.6 记忆

### 11.1 初次确认

当前 V1.6 以 5 分钟为期望间隔，初次正式确认要求 3 个连续原始异常点：

\[
entry\_consecutive=3
\]

即至少约 15 分钟的连续证据。

如果时间间隔不是 5 分钟，连续计数重置。

### 11.2 严格分层孤立规则

严格分支要求每个连续点都通过完整的分层 LOF 原始候选条件。只有方向为低、但 LOF 已跌回阈值以下的点，不能继续确认严格事件。

### 11.3 确认后的记忆

一旦某条组串已经由连续 3 点确认，记忆状态激活：

- 后续再次出现完整原始异常时，可以立即告警，不必重新等待 3 点；
- 中间出现正常点时，该正常点本身不告警；
- 前两个连续正常点只累计恢复计数，不立即清除记忆；
- 连续 3 个有效正常点后清除记忆；
- 数据缺口或不可评估点会清除状态，避免陈旧状态跨越缺失数据。

形式化状态可表示为：

```text
INACTIVE
  --3个连续异常--> ACTIVE

ACTIVE
  --异常--> 当前点告警，正常计数清零
  --1~2个连续正常--> 当前点不告警，但保留记忆
  --3个连续正常--> INACTIVE
  --数据缺口/不可评估--> INACTIVE
```

最终 V1.6 点级告警为：

\[
Alert^{V1.6}
=Alert^{hier\_strict}
\lor Alert^{memory}
\]

记忆机制只影响时间状态，不会把一个正常点强制写成异常点。

对应实现：

- `src/pv_anomaly/pvlof_v2_hier.py`；
- `src/pv_anomaly/pvlof_v15.py`；
- `src/pv_anomaly/pvlof_v16.py`。

---

## 12. 标定过程

当前完整标定至少包含以下内容：

1. 推断每台设备的真实组串清单；
2. 计算每台逆变器的 95% 设备尺度 \(S_d\)；
3. 构造留一逆变器虚拟辐照度；
4. 标定每条组串响应 \(a_{d,i}\)；
5. 标定全局基础 LOF 阈值；
6. 标定残差低分位阈值；
7. 标定群体最大断层阈值；
8. 标定 `forecast_ghi → virtual irradiance` 的电站映射；
9. 标定设备/电站/组串数分层 LOF 阈值；
10. 派生 V1.6 的物理门控配置和时间记忆配置。

已知告警区间会尽量从标定数据中排除。但由于旧告警本身来自固定阈值法、标签不完整，标定数据仍可能混有未知低流异常。

这会带来两个风险：

- 历史异常抬高 LOF 的 99.5% 阈值；
- 长期低电流被组串响应中位数吸收，使其期望电流也偏低。

---

## 13. 点级输出语义

常见诊断字段：

| 字段 | 含义 |
|---|---|
| `string_current` | 当前原始组串电流 |
| `virtual_irradiance` | 最终用于计算期望电流的无量纲虚拟辐照度 |
| `forecast_ghi` | 对齐后的天气预报水平总辐照度 |
| `expected_current` | 组串条件期望电流 |
| `residual_ratio` | 实际电流 / 期望电流 |
| `residual_median` | 同设备同时刻有效组串残差中位数 |
| `pvlof_score` | 一维残差 LOF 分数 |
| `isolated_relative_drop` | 相对同期残差中位数的下降比例 |
| `isolated_absolute_drop` | 期望电流减实际电流 |
| `isolated_hier_lof_threshold` | 当前上下文采用的分层 LOF 阈值 |
| `isolated_hier_threshold_level` | 阈值来自设备、电站+组串数、组串数或全局 |
| `collective_gap` | 当前选中的残差最大断层 |
| `collective_group_size` | 当前低侧群体大小 |
| `collective_group_median` | 低侧群体残差中位数 |
| `collective_raw_alert` | 当前组串是否属于原始群体候选 |
| `pvlof_v16_raw_anomaly` | V1.6 孤立原始异常证据 |
| `pvlof_v16_entry_streak` | 当前连续进入计数 |
| `pvlof_v16_memory_active` | 确认后的记忆是否激活 |
| `pvlof_v16_alert` | V1.6 最终点级告警 |

面向客户的宽表不会暴露全部中间字段，只显示时间、电站、设备、最终组串编号和必要的原始电流。中间字段主要用于研发诊断。

---

## 14. 事件语义

点级结果按电站、逆变器、组串和 5 分钟时间连续性合并为事件。

需要区分：

1. **候选点**：当前时刻满足原始异常条件；
2. **确认点**：候选连续性达到正式告警要求；
3. **记忆重触发点**：近期已经确认，记忆清除前再次出现原始异常；
4. **事件开始时间**：客户事件表可追溯到最终被确认的候选段起点；
5. **事件结束时间**：最后一个异常点，或按既定恢复语义确定的结束时间。

只有候选但最终没有达到确认要求的序列，不应进入正式客户告警事件表，但可以保留在研发审计表中。

---

## 15. 当前已经观察到的主要漏检模式

### 15.1 强物理欠流但 LOF 未过阈值

某组串实际电流和期望电流差异很大，但周围存在若干中度低流组串形成密度梯度，使 LOF 低于严格设备阈值。

原因：V1.6 中 LOF 是不可绕过的前置条件。

### 15.2 密集低电流群体的 LOF 接近 1

多条组串一起下降并形成紧密低流群体时，它们互相成为近邻，因此每条组串的 LOF 可能仍接近 1。

原因：LOF 检测局部密度异常，而不是物理缺口大小。

### 15.3 阶梯状、多层群体被单次最大断层掩盖

当残差呈现：

```text
严重低流 | 中度低流 | 正常高流
```

最大断层可能把“严重+中度”一起切到低侧。如果低侧比例超过 60%，当前算法拒绝整个群体，却不继续搜索低侧内部的次级断层。

### 15.4 多数或全部组串同步欠流

如果整台逆变器一起下降，内部残差中位数也一起下降，组串横向方法缺乏少数异常参照。

需要独立的设备级“目标逆变器 vs 同站其他逆变器”欠流分支。

### 15.5 长期低流被期望模型吸收

如果组串在标定历史中长期偏低，响应系数 \(a_{d,i}\) 也会偏低，导致异常状态被学习成正常响应。

### 15.6 少于 3 点的异常不形成正式告警

这是当前降低误报的有意设计，不应与“没有候选”混为一谈。

---

## 16. 当前已知边界与禁止误解

1. `LOF` 高不等于低电流；高电流或位于两个群体之间的过渡点也可能有高 LOF。
2. `LOF` 接近 1 不等于物理正常；密集异常群体也可能 LOF 接近 1。
3. `forecast_ghi` 不是实测辐照度，只能有限度辅助。
4. 当前群体检测只做单层最大断层，不是完整的多峰聚类。
5. 当前 V1.6 的 20% 与 0.5 A 门控是 OR，不是 AND。
6. 0 A 不属于本版本非零低流 PVLOF 的评价范围。
7. 当前告警 CSV 多数只包含正类告警窗口，不能仅凭该数据计算完整 Precision、F1 和真负例。
8. `device_alarm` 是旧阈值法输出，不等于经过人工确认的绝对真值，只适合作为弱标签和人工抽查索引。

---

## 17. 供后续 AI 讨论的改进方向（尚未实现）

以下只是待研究方向，不属于 V1.6：

### 17.1 强物理欠流救援

在期望电流可靠、相对和绝对缺口都极显著时，允许形成受控候选，而不让 LOF 成为唯一前置条件。为控制误报，建议使用比 V1.6 更严格的双重物理条件和正常数据分位数标定。

### 17.2 方向化 LOF 聚合

多个组串的 LOF 都略高但单个未过阈值时，可以聚合低方向证据，而不是直接求所有 LOF 的和。例如：

\[
A_i
=
\mathbf 1(r_i<\widetilde r)
\max(LOF_i-1,0)
\max\left(0,1-\frac{r_i}{\widetilde r}\right)
\]

\[
A_{device,t}=\sum_i A_i
\]

聚合阈值必须按设备和有效组串数量从正常数据标定。

### 17.3 递归或多断点群体分割

当最大断层因低侧比例过大被拒绝时，继续在低侧内部寻找次级显著断层，并使用物理缺口、健康高侧支持和时间连续性限制误报。

### 17.4 设备级整体欠流

比较目标逆变器残差中位数与同站其他逆变器，不输出具体组串，单独定义设备级告警类型。

### 17.5 健康响应上包络

避免长期低流被历史中位数吸收，可研究正常高质量时期的响应上包络或健康分位数，但需要防止不同组串配置造成系统性高估。

---

## 18. 对后续改进方案的最低要求

任何新版本至少应满足：

1. 不覆盖 V1.6 输出，使用并行结果列进行比较；
2. 保留真实通道清单，不能退回 `1 ... main_string_count` 假设；
3. 不能通过全局降低 LOF 阈值换取召回率；
4. 不能简单提高 60% 群体比例而不增加误报保护；
5. 新阈值应由正常历史数据标定，并明确回退层级；
6. 保留 5 分钟时间连续性和数据缺口处理；
7. 输出必须能定位具体组串，设备级异常需使用独立告警类型；
8. 使用已有人工案例做回归测试，检查新增召回和新增误报；
9. 在 234、791、892 三个生产电站以及测试环境 33 号电站上分别审计；
10. 评价时区分候选召回、正式事件召回、人工精度和弱标签一致率。

---

## 19. 主要实现文件

| 文件 | 作用 |
|---|---|
| `src/pv_anomaly/pvlof.py` | 一维 LOF 和 V1 基础实现 |
| `src/pv_anomaly/pvlof_channels.py` | 真实组串通道清单推断 |
| `src/pv_anomaly/pvlof_v2.py` | 虚拟辐照度、期望电流、残差、孤立和群体分支 |
| `src/pv_anomaly/pvlof_v12.py` | `forecast_ghi` 条件化与有界融合 |
| `src/pv_anomaly/pvlof_v2_hier.py` | 分层 LOF 阈值和严格连续分支 |
| `src/pv_anomaly/pvlof_v15.py` | 确认后记忆状态机 |
| `src/pv_anomaly/pvlof_v16.py` | V1.6 记忆配置封装 |
| `scripts/derive_pvlof_v16.py` | 派生 V1.6 的 20%/0.5 A OR 门控与时间配置 |
| `scripts/run_pvlof_v12_hier.py` | 天气条件化、V2 和分层严格检测主流程 |
| `scripts/apply_pvlof_v16_memory.py` | 在点级结果上应用 V1.6 记忆并重建事件 |

---

## 20. 一句话总结

PVLOF V1.6 使用同站其他逆变器和天气预报构造环境条件参考，把每条组串电流归一化为相对于自身历史响应的残差，再用分层 LOF 检测孤立低流、用排序残差最大断层检测少数低流群体，最后通过 3 点确认和确认后记忆生成稳定告警；当前最主要的结构性盲区是密集低流、多层阶梯群体、整机同步欠流以及历史长期低流被期望模型吸收。
