# 光伏遥测数据宽表数据契约

> 面向 AI 异常组串电流检测、横向组串对比和时间序列建模。
>
> 文档版本：v3
>
> 数据宽表：`pv_device_data`

## 1. 目标与范围

`pv_device_data` 将逆变器遥测、逆变器运行状态、逆变器额定功率和组串主备配置整合为一张 Elasticsearch 宽表。数据粒度为“一个逆变器、一个归整后的 5 分钟事件点、一条记录”。

主要用途：

- 组串电流低电流、零电流和离线异常检测。
- 同一逆变器主用组串的横向比较。
- 单逆变器和多逆变器的时间序列建模。
- 模型结果回溯到原始遥测、设备状态和组串配置。

当前同步范围由 `telemetry-wide-table.plant-ids` 配置。历史数据可按日期范围回写，默认回写近 180 天；如需全量历史，请按时间段分批触发历史回写任务。

## 2. 数据来源与关联

| 数据类别 | 来源 | 关联键 | 用途 |
|---|---|---|---|
| 遥测数据 | MongoDB `iot_device_data` | `deviceno`、`datats` | 组串、电压、电流、功率、温度等原始测量值 |
| 设备状态快照 | MongoDB `iot_device_status` | `deviceno`、`statustime` | 运行状态 `status_code` |
| 逆变器配置 | MySQL `sys_inverter_project` | `inverter_sn` | `plant_id`、`rated_power`、`string_count`、低电流比例阈值 |
| 组串配置 | MySQL `sys_inverter_string_config` | `project_id`、`string_no` | 主用/备用组串判定 |

逆变器的 `device_no` 等于 `sys_inverter_project.inverter_sn`，也等于遥测表中的 `deviceno`。

## 3. 时间与唯一性规则

### 3.1 时间字段

| 字段 | 类型 | 规则 |
|---|---|---|
| `event_time` | ES `date` | 按北京时间（`Asia/Shanghai`）输出的 ISO-8601 时间，例如 `2026-07-21T19:15:00+08:00` |
| `event_ts_ms` | `long` | 归整后的 5 分钟事件点毫秒时间戳 |

源遥测时间取 `iot_device_data.datats`。系统将源时间归整到**最近**的 5 分钟点：例如 `15:50:05` 归整为 `15:50:00`，`15:52:30` 归整为 `15:55:00`。

同一设备同一 5 分钟窗口存在多条遥测时，选取距归整点最近的一条；距离相同则取时间更新的一条。

### 3.2 文档 ID 与覆盖写入

ES `_id` 格式：

```text
{plant_id}:{device_no}:{event_ts_ms}
```

相同电站、设备和 5 分钟事件点始终覆盖写入。因此历史回写、增量重算和人工重跑不会产生重复文档；后到的状态快照或遥测数据可通过重算修正原记录。

## 4. 字段清单

ES 索引开启 `dynamic: false`。未在本协议内定义的字段不会写入，AI 侧应只依赖以下字段。

### 4.1 标识、时间和设备状态

| 字段 | ES 类型 | 含义 | 来源/计算方式 |
|---|---:|---|---|
| `plant_id` | `long` | 电站 ID | `sys_inverter_project.plant_id` |
| `device_no` | `keyword` | 逆变器 SN | `sys_inverter_project.inverter_sn` / `iot_device_data.deviceno` |
| `event_time` | `date` | 归整后事件时间，北京时间 | `datats` 归整后生成 |
| `event_ts_ms` | `long` | 归整后事件毫秒时间戳 | `datats` 归整后生成 |
| `status_code` | `integer` | 逆变器运行状态 | 优先匹配 `iot_device_status.runstatus`，缺失时由功率/电流兜底 |
| `rated_power` | `double` | 逆变器额定功率（kW） | `sys_inverter_project.rated_output_power` |

### 4.2 组串展开字段

| 字段族 | ES 类型 | 数量 | 原始字段 |
|---|---:|---:|---|
| `string_current_01` ... `string_current_30` | `double` | 30 | `dataobj.stringDataCurrent1` ... `stringDataCurrent30` |
| `string_voltage_01` ... `string_voltage_30` | `double` | 30 | `dataobj.stringDataVoltage1` ... `stringDataVoltage30` |
| `string_status_01` ... `string_status_30` | `integer` | 30 | 按第 6 节的组串状态规则计算 |

组串原始值仍完整保留。算法做主用组串横向比较时，必须结合 `string_status_XX`、`main_string_count` 和逆变器的实际组串数使用，不应把备用或物理不存在的组串纳入统计。

### 4.3 组串统计与异常标签

| 字段 | ES 类型 | 含义/口径 |
|---|---:|---|
| `average_current` | `double` | 主用组串中电流大于 0 的均值；无有效值时为 `null` |
| `average_voltage` | `double` | 主用组串中电压大于 0 的均值；无有效值时为 `null` |
| `max_string_current` | `double` | 主用组串非空电流最大值，包含 0 |
| `min_string_current` | `double` | 主用组串非空电流最小值，包含 0 |
| `main_string_count` | `integer` | 参与判定的主用组串数量 |
| `valid_current_string_count` | `integer` | 主用组串中电流大于 0 的数量 |
| `low_current_count` | `integer` | 判定为低电流的主用组串数 |
| `zero_current_count` | `integer` | 判定为零电流的主用组串数 |
| `string_overall_status` | `integer` | 本事件点的组串异常汇总状态，编码见第 5 节 |
| `current_dispersion_rate` | `double` | 原遥测电流离散率 | 
| `voltage_dispersion_rate` | `double` | 原遥测电压离散率 |

### 4.4 功率、电网与设备测量字段

| 字段 | ES 类型 | 原始字段 |
|---|---:|---|
| `dc_power` | `double` | `dataobj.dcPower` |
| `active_power` | `double` | `dataobj.activePower` |
| `total_power` | `double` | `dataobj.totalPower` |
| `reactive_power` | `double` | `dataobj.reactivePower` |
| `power_factor` | `double` | `dataobj.powerFactor` |
| `phase_voltage_a` / `phase_voltage_b` / `phase_voltage_c` | `double` | `dataobj.phaseVoltageA/B/C` |
| `phase_current_a` / `phase_current_b` / `phase_current_c` | `double` | `dataobj.phaseCurrentA/B/C` |
| `device_temperature` | `double` | `dataobj.deviceTemp` |
| `frequency` | `double` | `dataobj.frequency` |

原始值无法解析为数值时写入 `null`，不会以 `0` 代替。

## 5. 状态编码

### 5.1 逆变器状态 `status_code`

| 值 | 含义 | 来源 |
|---:|---|---|
| `0` | 离线 | `runstatus=0`，或状态快照缺失且全部功率/电流未超过兜底阈值 |
| `1` | 在线 | `runstatus=1`，或状态快照缺失但存在有效功率/电流 |
| `2` | 等待 | `runstatus=2` |
| `3` | 故障 | `runstatus=3` |
| `4` | 告警 | `runstatus=4` |

状态匹配按同一 `device_no` 的 `statustime` 与 `event_ts_ms` 的绝对时间差进行，默认最大窗口为 5 分钟（`status-match-window-minutes`）。选择窗口内最近的一条状态快照。

窗口内没有快照时，仅兜底判定在线或离线：依次检查 `active_power`、`dc_power`、`total_power`、三相电流和 30 路组串电流。默认功率/电流阈值为 `0.01`，可通过配置调整。

### 5.2 单组串状态 `string_status_01` ... `string_status_30`

| 值 | 含义 | 判定 |
|---:|---|---|
| `0` | 备用/不参与 | 非主用组串、超出 `string_count` 的组串，或未配置为主用 |
| `1` | 正常 | 主用组串且不满足异常条件 |
| `2` | 低电流 | 在线/告警状态下，满足低电流阈值 |
| `3` | 离线 | 逆变器离线或等待，或在线状态下该组串电流缺失 |
| `4` | 零电流 | 在线/告警状态下，满足零电流阈值 |

### 5.3 组串汇总状态 `string_overall_status`

| 值 | 含义 |
|---:|---|
| `1` | 无低电流、零电流异常 |
| `2` | 存在至少一个低电流组串 |
| `3` | 存在至少一个零电流组串 |
| `4` | 同时存在低电流和零电流组串 |

## 6. 主用组串、均值与异常判定

### 6.1 主用组串范围

只在以下范围内计算平均值、最大最小值和低/零电流异常：

1. 组串序号在 `1..min(sys_inverter_project.string_count, 30)` 内。
2. `sys_inverter_string_config.status = 1`，即主用组串。

`status = 2` 为备用，`status = 3` 为待确认，均不参与均值和异常统计。没有组串配置时，系统沿用现有业务口径，将所有组串视为备用，因此 `main_string_count` 为 `0`，组串统计字段为空或为 `0`。

### 6.2 电流异常规则

设主用且电流大于 0 的组串平均电流为 `average_current`：

- 只有当 `average_current > ems.string.check-current-threshold` 时，才开启低/零电流识别。默认阈值为 `0.5`。
- 当主用组串电流 `<= ems.string.check-current-zero-threshold` 时，标记为零电流（`4`）。默认阈值为 `0.5`。
- 当主用组串电流 `< average_current × sys_inverter_project.low_current_threshold` 时，标记为低电流（`2`）。
- `status_code=0` 或 `2` 时，主用组串直接标记为离线（`3`）；`status_code=3` 故障时，主用组串按现有页面规则标记为正常（`1`），不做低/零电流判断。
- `status_code=1` 或 `4` 时按上述低/零电流规则判定。

## 7. 推荐的 AI 使用口径

### 7.1 可作为组串电流异常检测候选的样本

建议至少满足以下条件：

```text
status_code IN (1, 4)
AND main_string_count > 0
AND valid_current_string_count > 0
AND average_current > 0
```

对单组串异常分类，可使用 `string_status_XX IN (1, 2, 4)` 的主用组串；`0`（备用）和 `3`（离线）不应作为低/零电流训练标签。

### 7.2 重要特征

- 组串相对电流：`string_current_XX / average_current`。
- 组串电压偏差：`string_voltage_XX - average_voltage` 或相对偏差。
- 逆变器负荷：`active_power / rated_power`，仅在 `rated_power > 0` 时计算。
- 同时序上下文：前后若干 5 分钟点的组串电流、功率、设备温度和离散率。
- 异常汇总：`low_current_count`、`zero_current_count`、`string_overall_status`。

应按 `plant_id + device_no` 分组、按 `event_ts_ms` 升序组织时间序列。避免将不同逆变器的组串序号直接横向比较；组串序号只在同一逆变器的主用组串范围内有可比性。

### 7.3 空值与边界处理

- `null` 表示源遥测缺失或无法解析，不等同于 `0`。
- 夜间或低辐照时，`average_current` 可能为 `null` 或很低；业务规则不会触发低/零电流标签，AI 侧应结合功率和时间上下文判断是否剔除。
- 备用、不存在或未配置主用的组串状态为 `0`，不是“零电流故障”。
- `status_code=3` 的故障设备不使用组串低/零电流标签，因为该状态下组串状态按业务规则置为正常。

## 8. 同步、回写与重算

### 8.1 历史回写

XXL Job Handler：`telemetryWideTableBackfillJobHandler`

| 参数 | 行为 |
|---|---|
| 空 | 按 `backfill-days` 回写，默认近 180 天 |
| `180` | 回写最近 180 个自然日 |
| `2026-01-01,2026-06-30` | 按指定日期范围回写 |

历史数据按天、按逆变器读取；跨越 180 天冷热库分界时，系统自动路由主 Mongo 和归档 Mongo。建议全历史以月或季度为单位分批执行，降低数据库与 ES 压力。

### 8.2 增量重算

XXL Job Handler：`telemetryWideTableJobHandler`

配置项：

```yaml
telemetry-wide-table:
  incremental-recompute-hours: -1
```

| 配置值 | 行为 |
|---:|---|
| `-1` | 默认关闭；调度任务记录跳过，不读写数据 |
| `1` | 每次重算当前时刻向前 1 小时的精确时间窗口 |
| `N`（正整数） | 每次重算当前时刻向前 N 小时 |
| `0` 或小于 `-1` | 非法配置，任务报错 |

推荐调度频率为每 5 分钟一次。建议在遥测和状态快照有入库延迟时，将回看窗口设为 `1` 至 `2` 小时。

### 8.3 手动接口

| 接口 | 方法 | 作用 |
|---|---|---|
| `/telemetryWideTable/sync?startDate=2026-01-01&endDate=2026-01-31` | `POST` | 按日期范围同步 |
| `/telemetryWideTable/backfill?days=180` | `POST` | 回写最近 N 个自然日 |
| `/telemetryWideTable/incremental` | `POST` | 按小时增量重算；受 `incremental-recompute-hours` 控制 |

## 9. ES 与 Mongo 运维要求

### 9.1 ES 索引

- 索引名：`pv_device_data`。
- 建议生产环境：`index-shards: 1`、`index-replicas: 0`，按实际高可用要求调整副本数。
- Bulk 写入默认 500 条一批。任一 Bulk 响应包含失败文档时，同步任务直接失败，避免部分失败被当作成功。

### 9.2 Mongo 索引

主 Mongo 和归档 Mongo 均应创建：

```javascript
db.iot_device_data.createIndex({ deviceno: 1, datats: 1 });
db.iot_device_status.createIndex({ deviceno: 1, statustime: 1 });
```

## 10. 宽表示例

```json
{
  "plant_id": 701,
  "device_no": "6T2459009447",
  "event_time": "2026-07-21T15:50:00+08:00",
  "event_ts_ms": 1784620200000,
  "status_code": 1,
  "rated_power": 110.0,
  "active_power": 72.5,
  "average_current": 8.42,
  "average_voltage": 650.1,
  "string_current_01": 8.51,
  "string_voltage_01": 651.3,
  "string_status_01": 1,
  "string_current_02": 1.20,
  "string_status_02": 2,
  "main_string_count": 16,
  "valid_current_string_count": 16,
  "low_current_count": 1,
  "zero_current_count": 0,
  "string_overall_status": 2
}
```

## 11. 交接确认项

AI 侧接入前，请确认以下内容：

1. 对于夜间、低辐照和低负荷时段，是否仅使用 `active_power` 或 `average_current` 达到指定阈值后的样本。
2. 是否将现有 `string_status_XX` 作为监督标签，还是仅作为规则标签/特征。
3. 是否需要将模型输出回写为独立 ES 索引或业务告警表；本宽表只承载输入特征和现有规则结果。
4. 若未来逆变器实际组串数超过 30，需要扩展 ES 映射和字段契约后再接入，当前协议固定支持 30 路。