# PROJECT_CONTEXT

> 用途：供另一个 AI 直接接手当前项目。  
> 整理日期：2026-07-22（Asia/Shanghai）  
> 来源：[ChatGPT 共享对话](https://chatgpt.com/share/6a602b39-9e94-83e8-9f89-8e08023bc9d1)，页面标题为“ChatGPT - 数据分析”。

## 1. 项目目标

构建一套面向光伏电站的异常检测与智能运维方案，从公司 Elasticsearch（下文简称 ES）中的光伏“宽表”读取时序数据，完成：

1. 用 Transformer 建模或预测光伏功率的正常行为；
2. 对模型残差使用 EWMA，检测电站或逆变器层面的整体异常；
3. 使用 PVLOF/LOF 比较同一时刻各组串的电流表现，定位异常组串；
4. 后续视权限与业务需求，将异常结果写回 ES，并用 Kibana 或其他界面展示、查询和告警。

当前阶段的首要目标不是继续扩展算法，而是验证 ES 可访问性并摸清数据结构，判断上述路线是否具备数据基础。

## 2. 背景与业务语境

- 项目处于光伏电站异常检测/智能运维场景。
- 公司内部使用 ES 保存已加工的业务宽表数据。这里的 “Elastic” 指 Elasticsearch 数据平台，不是 Elastic Net。
- “宽表”指将天气、功率、设备、组串等多来源信息按时间和设备维度提前关联、展开为字段很多的记录；ES 中通常表现为一个 index 里的 JSON document，而非关系数据库中的传统表。
- 理想情况下，一条记录代表“某电站/逆变器在某一时刻的完整运行状态”。
- 对话中曾多次上传文章并要求分析，但共享页面仅保留“上传文件”占位符，没有文章正文、文件名或分析结果。接手 AI 不得假定已知这些文章内容，若文章仍重要，需要用户重新提供。

## 3. 当前方案

### 3.1 总体数据流

```text
公司远程 Elasticsearch
        |
        | 查询光伏宽表
        v
Python 客户端 / pandas DataFrame
        |
        +--> 数据质量检查、时间对齐、缺失值处理
        |
        +--> Transformer：学习/预测正常功率
        |          |
        |          v
        |       预测残差
        |          |
        |          v
        |       EWMA：整体异常检测
        |
        +--> 同时刻各组串电流特征
                   |
                   v
                PVLOF/LOF：异常组串定位
                   |
                   v
        可选：写回告警 index / Kibana 展示
```

### 3.2 各组件职责

- **Elasticsearch**：原始/加工宽表的数据源；未来也可保存检测结果。它是数据平台层，不替代项目的诊断算法。
- **Transformer**：根据辐照度、温度、时间信息、历史功率等特征学习正常功率行为并输出预测值。确切模型结构尚未决定。
- **EWMA**：对实际功率与预测功率之间的残差进行平滑和阈值检测，用于识别系统级或逆变器级异常。参数必须结合采样周期设定。
- **PVLOF/LOF**：在同一时刻比较同一逆变器/MPPT 下的组串电流，识别离群组串，实现定位。具体 PVLOF 定义、特征和阈值尚未定稿。
- **Kibana/告警层**：可选的展示与运维层，不是当前算法验证的前置条件。

### 3.3 预期宽表字段

以下是对话中的候选字段，不代表实际 ES mapping 已确认：

| 类别 | 候选字段/含义 |
|---|---|
| 主键维度 | `timestamp`、`station_id`、`inverter_id`，可能还需 `mppt_id` |
| 功率 | `power`、`active_power`、`ac_power`、`dc_power` |
| 天气/环境 | `irradiance`、`radiation`、`module_temperature`、`ambient_temperature` |
| 组串电流 | `string1_current` ... `string17_current`，或 `string_current_1`、`pv01_i` 等其他命名 |
| 组串/直流电压 | `string_voltage_*`、`dc_voltage` |
| 告警 | `alarm_code` 等 |

字段名、字段类型、单位、缺失率、粒度、设备层级和实际组串数量全部需要从 mapping 与样例数据确认。

## 4. 已经完成的内容

目前完成的是概念澄清和实施路线讨论，尚未看到实际代码、数据或模型产物。

- 已确认公司数据平台是远程 ES，数据以宽表形式存储。
- 已解释宽表与窄表的区别，以及宽表对 Transformer 和 PVLOF 的便利性。
- 已形成 `ES -> pandas -> Transformer -> EWMA -> PVLOF -> 告警/展示` 的候选处理链。
- 已明确本地通常无需安装 Elasticsearch 服务，只需安装 Python 客户端并连接远程 ES。
- 已列出接入后的首轮探查任务：连通性、index、mapping、样例、采样周期、数据量、拓扑。
- 已识别关键可行性门槛：如果数据没有逐组串电流，PVLOF 组串定位无法按当前路线实施。

尚未完成：

- 未验证 ES 连接；
- 未获取 index 列表或 mapping；
- 未查看任何真实数据样例；
- 未确认采样周期、历史跨度、数据量和设备拓扑；
- 未完成清洗、EDA、模型、阈值、回测、告警写回或可视化；
- 未建立项目代码仓库或确认现有文件结构。

## 5. 技术栈

### 已明确

- Elasticsearch（公司远程服务）
- Python
- `elasticsearch` Python 客户端（版本需与服务端兼容）
- pandas
- Transformer（具体实现框架未定）
- EWMA
- LOF/PVLOF

### 可选/后续

- Kibana：展示原始曲线、异常结果和告警
- 独立告警 index，例如 `pv_alarm_index`（名称未确认）

### 未确定，不能擅自假设

- Python 版本和依赖管理方式；
- Transformer 使用 PyTorch、TensorFlow 还是其他框架；
- ES 服务端版本、TLS、证书、代理、VPN、兼容模式；
- 实际 index 名、mapping、字段命名和权限；
- PVLOF 的准确论文/公式、输入特征与实现方式；
- 训练/验证/测试切分方式及异常标签来源；
- 部署方式、调度频率、实时性和告警 SLA。

## 6. ES 连接信息与安全状态

共享对话给出了一个内网 ES 端点、端口、用户名和明文密码。为了避免继续传播凭据，本文件只保留必要的非密码上下文：

```yaml
host: 192.168.180.200
port: 30092
username: elastic
password: "[REDACTED — 不要写入代码或文档]"
```

安全要求：

- 该密码已经出现在共享对话中，应视为已暴露；在继续开发前由系统所有者轮换凭据。
- 优先申请只读、最小权限的项目账号，不应继续使用高权限通用账号。
- 凭据通过环境变量、密钥管理服务或本地未纳入版本控制的配置注入。
- 不在日志、异常、截图、Notebook 输出、Git 历史或交接文档中打印密码。
- 未获得写权限和业务批准前，只做只读查询，不创建/删除 index，不写回数据。
- 该地址是私网地址，可能需要在公司网络、VPN 或跳板机环境中访问。

推荐的配置接口（变量名可调整）：

```text
ES_URL=http://192.168.180.200:30092
ES_USERNAME=<least-privilege-user>
ES_PASSWORD=<injected-secret>
ES_INDEX=<confirmed-index-name>
```

## 7. 文件结构

共享对话没有提供任何现有仓库、文件名或目录树，因此不能声称下列结构已经存在。若从零开始，建议采用：

```text
project-root/
├─ README.md
├─ pyproject.toml              # 或 requirements.txt；二选一并固定版本
├─ .env.example                # 只写变量名，不写真实凭据
├─ .gitignore
├─ configs/
│  ├─ data.example.yaml
│  └─ model.example.yaml
├─ src/
│  └─ pv_anomaly/
│     ├─ __init__.py
│     ├─ es_client.py          # 只读连接、分页查询、mapping 获取
│     ├─ schema.py             # 字段别名、类型和单位校验
│     ├─ preprocessing.py      # 时间对齐、缺失值、夜间/低辐照过滤
│     ├─ transformer.py        # 正常行为预测
│     ├─ ewma.py               # 残差监控
│     ├─ pvlof.py              # 组串离群定位
│     └─ pipeline.py           # 组合流程
├─ scripts/
│  ├─ check_es_connection.py
│  ├─ inspect_indices.py
│  ├─ inspect_mapping.py
│  └─ export_sample.py
├─ notebooks/
│  └─ 01_data_audit.ipynb
├─ tests/
│  ├─ test_schema.py
│  ├─ test_ewma.py
│  └─ test_pvlof.py
├─ data/
│  ├─ raw/                     # 默认不入 Git
│  └─ processed/               # 默认不入 Git
└─ docs/
   ├─ PROJECT_CONTEXT.md
   └─ DECISIONS.md
```

如果用户已有仓库，应先读取真实目录和现有约定，再决定是否采用这套结构；不得直接覆盖。

## 8. 尚未解决的问题

### 数据接入

1. ES 是否可从当前开发环境访问？是否要求 VPN、HTTP/HTTPS、证书或代理？
2. ES 服务端版本是什么？应安装哪个兼容版本的 `elasticsearch` Python 客户端？
3. 可访问的 index 有哪些？目标宽表 index 的准确名称是什么？是否使用 alias/data stream？
4. 当前账号的实际权限范围是什么？能否只读 mapping、search、scroll/PIT？是否允许未来写告警？

### Schema 与数据质量

5. 实际 mapping、字段类型、字段单位、时区是什么？
6. 一行数据的粒度是电站、逆变器、MPPT 还是组串？是否存在重复记录？
7. 采样周期是 1 秒、1 分钟、5 分钟、15 分钟，还是不规则采样？
8. 是否同时包含功率、辐照度、温度、逐组串电流和电压？
9. 有多少电站、逆变器、MPPT、组串，历史跨度和缺失率怎样？
10. 是否有设备拓扑表，能把 `station -> inverter -> MPPT -> string` 对应起来？
11. 是否有维护记录、故障码或人工确认标签可用于评估？

### 算法设计

12. Transformer 的预测目标、输入窗口、预测步长和特征集合是什么？
13. 正常训练数据如何筛选，如何避免异常污染训练集？
14. EWMA 监控原始残差、标准化残差还是相对误差？控制限和持续时间规则如何定？
15. PVLOF 的准确方法定义是什么？是在单时刻横向比较，还是加入历史/天气归一化特征？
16. 夜间、低辐照、限电、遮挡、清洗、停机、通信丢包如何区分？
17. 评估指标和验收标准是什么（误报率、漏报率、提前量、定位准确率等）？

### 产品化

18. 是离线研究、定时批处理还是准实时服务？
19. 告警结果写回哪个 index，使用什么 schema、幂等键、保留期和状态流转？
20. Kibana/其他 UI 是否在项目范围内？告警由谁确认、关闭和反馈？

## 9. 下一步行动

### P0：安全与访问前置

1. 让 ES 所有者轮换共享对话中暴露的密码。
2. 申请一个只读、最小权限的项目账号，并确认网络接入方式。
3. 确认 ES 服务端版本和连接协议，选择兼容客户端版本。

### P1：只读数据探查

4. 用最小脚本验证连接；不要在输出中打印认证信息。
5. 获取允许访问的 index/alias 列表。
6. 获取目标 index 的 mapping 和少量脱敏样例。
7. 选一个电站/逆变器，导出一天数据，检查采样周期、时区、重复、缺失和字段单位。
8. 画出功率、辐照度及所有组串电流曲线，验证数据是否适合当前算法路线。
9. 获取电站拓扑与历史故障/维护信息。

### P2：冻结数据契约与基线

10. 建立字段别名表、单位规范、schema 校验和只读查询模块。
11. 先完成简单基线与数据质量规则，再实现 Transformer：例如辐照度归一化、同组串中位数偏差、基础 LOF/EWMA。
12. 明确训练/验证/测试按时间和电站切分，避免未来数据泄漏。

### P3：完整建模与闭环

13. 训练 Transformer 正常行为模型并生成残差。
14. 在验证集上标定 EWMA 和 PVLOF 阈值，加入持续时间、夜间和低辐照过滤。
15. 使用真实故障/维护记录进行离线回测。
16. 只有在只读验证通过、获得写权限并完成 schema 评审后，才设计告警写回与 Kibana 展示。

## 10. 不能丢失的约束

1. **Schema-first**：在 mapping、样例、采样周期和拓扑确认前，不应承诺当前算法一定可落地。
2. **组串电流是 PVLOF 的硬前提**：没有逐组串电流时，只能保留 Transformer + EWMA 或重新设计定位方案。
3. **本地默认不安装 ES 服务**：公司已有远程 ES；本地只需客户端。只有离线模拟/集成测试有明确需求时才考虑本地 ES。
4. **远程 ES 首先只读**：没有明确授权时，不写回、不创建、不修改、不删除 index。
5. **凭据必须脱敏和轮换**：共享对话中的明文密码不能继续使用或复制到代码。
6. **参数依赖采样周期**：EWMA 的平滑参数、窗口、控制限和告警持续时间不能脱离数据频率设置。
7. **设备拓扑必须保留**：组串离群只能在合理的同类比较集合中进行，至少需要明确电站、逆变器、MPPT、组串关系。
8. **算法层与平台层分离**：Transformer/EWMA/PVLOF 负责诊断；ES/Kibana 负责存储、查询、展示和承载结果。
9. **不臆造缺失资料**：已上传文章、真实数据、mapping、模型代码和实验结果在共享对话中均不可见或尚不存在。
10. **防止时间泄漏**：时序训练、验证和测试必须按时间/站点合理切分；预处理统计量只能由训练集拟合。
11. **先做数据质量与简单基线**：不要在数据尚未审计时直接投入复杂 Transformer。

## 11. 接手 AI 的建议首条回复/动作

先向用户索取或在授权环境中只读获取以下四样东西：

1. 目标 ES index/alias 名称；
2. mapping；
3. 少量脱敏 document 样例或一天数据；
4. 电站—逆变器—MPPT—组串拓扑。

拿到后先做数据可行性审计，并明确回答：

- Transformer 的目标和输入字段是否齐全；
- EWMA 可监控的残差/指标是什么；
- 是否具备逐组串电流与合理比较组，从而支持 PVLOF 定位；
- 还缺什么字段、标签或权限。
