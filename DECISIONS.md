# DECISIONS

> 本文件记录共享对话中讨论过的方案、当前选择及理由。  
> 状态说明：`已决定` 表示当前主线应继承；`暂定` 表示可随真实数据修正；`未决定` 表示接手 AI 不得自行宣称已有结论。

## D-001：Elastic 的含义

- **状态**：已决定
- **讨论过的解释**：Elastic Stack/Elasticsearch 数据平台；Elastic Net 正则化算法。
- **最终选择**：这里指公司内部用于保存光伏业务宽表的 Elasticsearch。
- **理由**：用户明确说明程序通过 ES host、port、username、password 连接一个存宽表数据的平台。
- **影响**：工作重点是远程数据接入和 schema 探查，而不是讨论 Elastic Net。

## D-002：是否在本地安装 Elasticsearch

- **状态**：已决定（默认策略）
- **候选方案**：
  1. 本地安装完整 Elasticsearch/Kibana；
  2. 本地只安装 Python 客户端，连接公司的远程 ES。
- **最终选择**：默认采用方案 2。
- **理由**：ES 服务已经部署在公司服务器；开发机是客户端，连接远程服务不要求本地再运行一套 ES。
- **例外**：只有需要完全离线的模拟环境、可重复集成测试或公司明确要求时，才考虑本地/容器化 ES。

## D-003：算法与数据平台的职责边界

- **状态**：已决定
- **候选方案**：让 Elastic 自带 ML 直接承担全部异常检测；保留自研算法并把 ES 作为平台。
- **最终选择**：ES/Kibana 承担数据存储、查询、展示和结果承载；Transformer + EWMA + PVLOF 承担可解释的智能诊断。
- **理由**：研究目标需要整体异常检测和组串定位；自研路线更容易结合光伏机理和设备层级解释。
- **影响**：Elastic ML 可作为对照或辅助，不应未经验证直接替代现有研究路线。

## D-004：当前候选算法链

- **状态**：暂定，取决于真实数据
- **最终选择**：
  1. Transformer 预测正常功率；
  2. EWMA 监控预测残差，发现整体异常；
  3. PVLOF/LOF 比较组串电流，定位异常组串。
- **理由**：这三层分别覆盖正常行为建模、系统异常检出和局部定位，职责互补。
- **尚未决定**：Transformer 架构、损失函数、输入窗口；EWMA 标准化和控制限；PVLOF 公式、特征和阈值。

## D-005：采用宽表作为算法输入接口

- **状态**：暂定（需 mapping 验证）
- **候选方案**：运行时查询多个窄表并 JOIN；直接读取提前加工的 ES 宽表。
- **最终选择**：优先读取公司已有宽表。
- **理由**：宽表能把同一时刻的天气、功率和组串测量对齐，降低在线 JOIN 和时间对齐复杂度，适合 DataFrame 和模型特征读取。
- **风险**：宽表可能存在字段命名混乱、类型漂移、重复、单位不一致和维度过宽；必须通过 mapping 和数据审计控制。

## D-006：PVLOF 的数据可行性门槛

- **状态**：已决定
- **最终选择**：只有存在逐组串电流，并能确定合理的同类比较集合时，才实施 PVLOF 组串定位。
- **理由**：PVLOF/LOF 依赖同一时刻多条组串的横向对比；只有汇总功率或逆变器总电流不足以定位具体组串。
- **降级方案**：缺少逐组串电流时，先保留 Transformer + EWMA；若仍需定位，必须寻找其他传感器、告警码或重新设计方法。

## D-007：建模前先做 schema 与数据审计

- **状态**：已决定
- **候选方案**：先写/训练模型；先确认数据结构和质量。
- **最终选择**：先连接 ES、查看 index、mapping、样例、采样周期、数据量和拓扑，再冻结模型方案。
- **理由**：字段是否存在、采样粒度、数据历史和设备关系直接决定模型输入、窗口、阈值与可评估性。
- **影响**：下一里程碑是“数据可行性报告”，不是“训练完成”。

## D-008：首轮 ES 操作必须只读

- **状态**：已决定
- **最终选择**：首轮仅做连接测试、列出授权 index/alias、读取 mapping 和少量样例；不创建、更新或删除任何远程对象。
- **理由**：当前没有写权限说明、目标告警 schema 或数据治理约定；误操作工业数据平台的风险高。
- **升级条件**：业务明确要求写回、提供最小写权限、审核目标 index/schema/幂等策略后，才实现写入。

## D-009：凭据处理

- **状态**：已决定
- **讨论事实**：共享对话中出现了 ES 明文密码。
- **最终选择**：交接材料中密码脱敏；要求轮换已暴露凭据，并使用环境变量或密钥管理注入。
- **理由**：共享链接扩大了凭据暴露面，继续复制会放大安全风险。
- **约束**：不得把真实密码写入代码、配置样例、Notebook、日志或版本控制。

## D-010：EWMA 参数必须依据采样周期

- **状态**：已决定原则，参数未决定
- **最终选择**：在采样周期确认后，以业务时间尺度定义窗口、平滑参数、控制限和持续时间规则。
- **理由**：同样的点数在 1 分钟、5 分钟和 15 分钟数据上代表完全不同的物理时长。
- **示例**：若是 5 分钟采样，30 分钟窗口对应 6 个点；这只是说明换算方式，不是最终参数。

## D-011：Transformer 技术框架

- **状态**：已决定
- **最终选择**：PyTorch 2.12.x，Linux 训练服务器使用 CUDA 13.0 运行时；依赖由 uv 和 `uv.lock` 管理。
- **理由**：PyTorch 2.12 的稳定 Linux wheel 支持 Blackwell/`sm_120`，与 RTX 5090 D 和服务器 595.71.05 驱动兼容；锁文件同时保留不同平台所需的 wheel。
- **验证要求**：服务器同步依赖后必须运行 `scripts/verify_torch_gpu.py`，确认设备名称、计算能力、`sm_120` 架构和实际 CUDA 矩阵计算均正常。
- **首版模型**：24 个连续 5 分钟点预测 `t+5min` 有功功率，训练集拟合缩放器和设备词表，Smooth L1 损失、AdamW、混合精度、验证集 RMSE 早停。

## D-012：Kibana 和告警写回

- **状态**：暂定为后续阶段
- **候选方案**：只做离线算法研究；把异常结果写回 ES 并在 Kibana 展示。
- **当前选择**：先完成只读接入和离线验证；产品化阶段再决定告警 index 与 Kibana。
- **理由**：目前连真实数据结构和模型有效性都未确认，提前设计写回容易造成返工和权限风险。

## D-013：实施顺序

- **状态**：已决定
- **最终顺序**：
  1. 安全整改与只读访问；
  2. index/mapping/样例/拓扑探查；
  3. 数据质量审计和简单基线；
  4. Transformer、EWMA、PVLOF 分层验证；
  5. 离线回测与阈值标定；
  6. 获批后再做写回、展示和告警闭环。
- **理由**：每一步为下一步提供可验证的前置条件，能尽早暴露缺字段、缺标签和不可比较等问题。

## D-014：早期上传文章的处理

- **状态**：已决定的信息边界
- **事实**：共享页面可见多条“上传文件/详细分析这篇文章”的消息，但附件正文、文件名及其回答不可见。
- **最终选择**：不把那些文章当作已掌握的项目资料，也不推断它们与当前算法路线的具体关系。
- **下一步**：若这些文章是方法依据，请用户重新上传或提供可访问链接，并单独提炼文献结论、公式和实现约束。

## 仍需用户/数据负责人拍板的事项

1. 项目最终交付物是论文实验、离线分析工具还是在线生产系统；
2. 目标数据 index 与账号权限；
3. 设备粒度、拓扑和字段数据字典；
4. Transformer 的目标、框架和评估方案；
5. PVLOF 的权威定义/参考文献；
6. 异常标签和验收指标；
7. 是否以及何时把结果写回 ES；
8. Kibana/告警闭环是否纳入本期范围。
